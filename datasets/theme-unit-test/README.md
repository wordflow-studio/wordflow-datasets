# Theme Unit Test Subset

`theme-unit-test` is a small normalized subset derived from the upstream WordPress Theme Test Data project.

This repo keeps the subset intentionally small:

- one page from the "about the tests" flow
- one accessibility-oriented article
- one formatting-heavy article

The dataset preserves pinned provenance to the upstream XML and README while reducing the content to the minimum needed for validation and repeatable apply tests.
